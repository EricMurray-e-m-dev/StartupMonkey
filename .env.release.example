# =============================================================================
# StartupMonkey Configuration
# =============================================================================
# Copy this file to .env and configure your database connection.
#
# Quick Start:
#   cp .env.release.example .env
#   nano .env  # Edit with your database details
#   docker-compose -f docker-compose.release.yml up -d
#   open http://localhost:3000
# =============================================================================

# -----------------------------------------------------------------------------
# TARGET DATABASE (REQUIRED)
# -----------------------------------------------------------------------------
# The PostgreSQL database you want StartupMonkey to monitor and optimise.
# StartupMonkey needs read access for monitoring and write access for creating
# indexes and applying optimisations.
#
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=disable
#
DB_CONNECTION_STRING=postgresql://your_user:your_password@your-db-host:5432/your_database?sslmode=disable

# Friendly name for this database (shown in dashboard)
DATABASE_ID=my-production-db
DATABASE_NAME=production

# Database type (currently only postgres supported)
DB_ADAPTER=postgres

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# How often to collect metrics from your database
# Lower = more responsive detection, higher = less overhead
# Default: 30s
# COLLECTION_INTERVAL=30s

# Dashboard port (default: 3000)
# DASHBOARD_PORT=3000

# Redis password for StartupMonkey's internal state store
# Leave empty for no password (fine for local/internal deployment)
# REDIS_PASSWORD=